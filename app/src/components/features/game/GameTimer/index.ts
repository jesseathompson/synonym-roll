export { GameTimer } from './GameTimer';
export type { GameTimerProps } from './GameTimer';

// Default export for convenience
import { GameTimer as DefaultGameTimer } from './GameTimer';
export default DefaultGameTimer;
